{
  "devDependencies": {
    "eslint": "^3.0.1"
  },
  "scripts": {
    "up": "db-migrate up",
    "create": "db-migrate create",
    "reset": "db-migrate reset"
  },
  "dependencies": {
    "db-migrate": "^0.10.0-beta.15",
    "db-migrate-pg": "^0.1.10",
    "express": "^4.14.0",
    "pg": "^6.0.3"
  }
}
